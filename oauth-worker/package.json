{
  "name": "tcmt-decap-oauth",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker: GitHub OAuth provider for Decap CMS on the TCM Technology site.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
