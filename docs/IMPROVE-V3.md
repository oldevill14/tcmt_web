# TCM Website — IMPROVE V3 (ui-ux-pro-max guided)

Source of truth: `design-system/tcm-technology/MASTER.md` (generated by ui-ux-pro-max for a B2B IT
hardware-vendor + software-dev company). This file = the gap analysis + concrete tasks to close it.

## What ui-ux-pro-max prescribes (and we must add)
- **Pattern = "Enterprise Gateway"**: Hero → **Solutions by Sector (tabbed)** → Solutions by Role → **Client Logos** → **Contact Sales**. Trust signals prominent. Primary CTA = **Contact Sales / ขอใบเสนอราคา**.
- **Style = "Trust & Authority"**: certifications/badges, **authorized-partner** signals, credentials, case studies with scope, security/quality badges, industry recognition, smooth stat reveal.
- **Colors**: professional **navy #0F172A + blue CTA #0369A1**, conservative accents. **AVOID AI purple/pink gradients** (keep blue→cyan, ensure it never drifts purple/magenta).
- **Typography**: clean corporate (Lexend/Source Sans 3 are ideal; our Inter + IBM Plex Sans Thai are acceptable — keep, but tighten).
- **UX checklist (CRITICAL/HIGH)**: visible focus rings (`focus:ring-2`), `cursor-pointer` on all clickable, hover feedback 150–300ms, 44px touch targets, SVG icons (NO emoji), `prefers-reduced-motion`, light-mode text contrast ≥4.5:1, no horizontal scroll, responsive at 375/768/1024/1440.

## Current state (v2) — gap analysis
✅ Have: modern aurora hero, services bento, client/partner logos, stats, CTA, scroll-reveal, reduced-motion.
⚠️ MISSING (this pass closes these):
1. **Trust & Authority layer** — no "Authorized Partner" framing, no credentials/certification band, no security/quality signals, no case-study scope on projects. (Biggest gap.)
2. **Solutions by Sector** (Enterprise Gateway) — no gov / education / enterprise segmentation with tab switching.
3. **Contact Sales** not positioned as the clear primary conversion.
4. a11y/checklist not fully audited (focus rings, cursor-pointer, contrast).

## TASKS

### T1 — Trust & Authority layer
- **Authorized-partner strip**: reframe the partner marquee with a heading like "ตัวแทนจำหน่าย/พันธมิตรที่ได้รับการแต่งตั้งจากแบรนด์ชั้นนำ" / "Authorized partner of leading global brands". Real logos already in `/logos/`.
- **Credentials / trust band** near top (under hero): badges — "ก่อตั้งปี 2541 / 25+ ปี", "ไว้วางใจโดยหน่วยงานภาครัฐ", "บริการ 24/7", "System Integrator ครบวงจร". Use SVG icons (shield/check/award), subtle hover.
- Keep claims credible — do NOT invent certifications you can't verify; use what's true (years, gov-trusted, SI). Mark "ISO / vendor certifications" as a TODO placeholder only if clearly labeled.

### T2 — Solutions by Sector (Enterprise Gateway)
- New section: tabbed/segmented **ภาครัฐ (Government) · สถาบันการศึกษา (Education) · องค์กรเอกชน (Enterprise)**. Each tab shows the relevant solutions/services + a short value line. CSS-only or tiny progressive-enhanced JS (reduced-motion safe, keyboard accessible, ARIA tabs).

### T3 — Projects as case studies
- Project cards/pages: add **sector chip + scope** (services delivered) framing. Credible, no fabricated metrics.

### T4 — Contact Sales primary CTA
- Make "ปรึกษาทีม / ขอใบเสนอราคา" the visually primary CTA (header button + hero + CTA banner). Secondary = ดูบริการ.

### T5 — ui-ux-pro-max checklist audit (whole site)
- focus-visible rings on all interactive; `cursor-pointer` on cards/links; hover transitions 150–300ms; 44px touch targets; verify NO emoji-as-icon (SVG only); light-section text ≥4.5:1; confirm gradient stays blue→cyan (no purple/pink); responsive 375/768/1024/1440; reduced-motion respected.

## Guardrails
- Keep static Astro + GitHub Pages base `/tcmt_web` (withBase/localizePath), bilingual th/en parity, build green, all copy via ui.ts/content (no hardcoded display strings). Do not break v2 — evolve it.
